.container
  .row
    .col-12
      .message-actions.my-2
        %a{href: new_message_path, class: "btn btn-primary"}
          New Message
  .row
    .col-6
      %h4
        Inbox

      - @inbox.each do |msg|
        .message
          = link_to message_path(msg.id) do
            %h5
              = msg.subject
    .col-6
      %h4
        Outbox

      - @outbox.each do |msg|
        .message
          = link_to message_path(msg.id) do
            %h5
              = msg.subject
